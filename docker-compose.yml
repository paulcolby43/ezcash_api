version: '3'
services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "3000:3000"
    image: tranact/ezcash_api_app
    # environment:
    #  - EZCASH_PASSWORD=${EZCASH_PASSWORD}
